# Odin/MPI Makefile for MapReduce programs

OUTOFCORE = -DLOCALDISK=/localdisk1/scratch -DNEW_OUT_OF_CORE

CC =		mpic++ -m64 $(OUTOFCORE)
CCFLAGS =	-O2 -I../new 
LINK =		mpic++ -m64 $(OUTOFCORE)
LINKFLAGS =	-O2
USRLIB =	-L../new -lmrmpi
SYSLIB =	
LIB =		../new/libmrmpi.a
DEPFLAGS =      -M

include Makefile.common

